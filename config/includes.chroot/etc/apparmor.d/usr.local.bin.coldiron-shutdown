# COLDIRON OS — AppArmor profile for the secure shutdown.
abi <abi/4.0>,
include <tunables/global>

profile /usr/local/bin/coldiron-shutdown {
  include <abstractions/base>
  include <abstractions/bash>

  deny network,

  /{usr/,}bin/bash mr,   # interpreter mmap under px transitions

  /usr/local/bin/coldiron-lib r,
  /{usr/,}bin/umount ux,
  /{usr/,}sbin/cryptsetup ux,
  /{usr/,}bin/sync ux,
  /{usr/,}bin/systemctl ux,
  /{usr/,}sbin/poweroff ux,
  /usr/local/bin/coldiron-* r,   # bash reads the script under the profile
  /dev/tty rw,                   # interactive menu I/O
  /dev/ttyS0 rw,                 # serial console (E2E / remote)
  /{usr/,}bin/cat ux,
  /{usr/,}bin/cut ux,
  /{usr/,}bin/grep ux,
  /{usr/,}bin/head ux,
  /{usr/,}bin/tail ux,
  /{usr/,}bin/chmod ux,
  /{usr/,}bin/rm ux,
  /{usr/,}bin/mountpoint ux,
}
