# COLDIRON OS — only apply the default-deny policy when the kernel has a
# packet filter at all. The networkless kernel (CONFIG_NETFILTER=n) has no
# /proc/net/netfilter, so nftables.service is skipped instead of failing.
[Unit]
ConditionPathExists=/proc/net/netfilter
